{
  "name": "email-worker",
  "version": "1.0.0",
  "description": "Email digest worker for GitHub Actions — sends via Nodemailer, reads from Supabase.",
  "main": "send-email.js",
  "scripts": {
    "start": "node send-email.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "nodemailer": "^6.9.0",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
